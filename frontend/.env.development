# Development environment variables
# These are used when running `npm run dev` locally

# For local development, use empty string to enable Vite proxy
# The Vite dev server will proxy /api and /socket.io requests to http://localhost:5001
VITE_API_BASE=

# Google OAuth Client ID (for local testing)
VITE_GOOGLE_CLIENT_ID=77809119535-r5ivv8inbitphbo2rcf5bik2d7grgf5g.apps.googleusercontent.com
